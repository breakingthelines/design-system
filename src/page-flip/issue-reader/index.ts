// Programme Issue reader — the standalone "open an issue" experience, composed
// over the PageFlip runtime (it does NOT fork the flip). Reused later for the
// onboarding Issue #1 reveal.

export { IssueReader } from './issue-reader';
export type {
  IssueReaderProps,
  IssueReaderHandle,
  IssueReaderMode,
  IssueMeta,
  IssueFace,
} from './issue-reader';
